# OT Review Pass 64

Scope: bounded cleanup for remaining repeated relationship/place title formulas.

Applied:
- `from/to/before sons of` -> `from/to/before the sons of`.
- `from/to daughters of` -> `from/to the daughters of`.
- `in cities of`, `in valley of`, `to king of`, and `to man of God` -> article-normalized forms.

Deferred:
- Bare `sons of Israel` and broader genealogy headings remain unchanged unless governed by a preposition.
- `City of David` capitalization remains untouched pending separate proper-name/location review.
